"""Summarizes the consequences of actions each day."""


class WorldModel:
    """Uses LLMs to summarize the consequences of actions each day."""

    def __init__(self, world_model_name: str) -> None:
        """Load a backend for the model."""
